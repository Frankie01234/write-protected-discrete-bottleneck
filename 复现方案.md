# 写保护离散瓶颈：修复语言接地世界模型 — 复现方案

> **等级：B（论文子集）**  
> **审查卡：** `...\审查卡\写保护离散瓶颈：修复语言接地世界模型_26.07.10_卡.md`  
> **论文：** https://arxiv.org/abs/2607.08312  
> **官方代码：** 未发现（按论文自实现）  
> **环境：** Grid world（必做） / MuJoCo desktop（可选）

## 1. 目标与口径

**目标：** 复现结构性负结果与三层足够修复：`z.detach()`、Memory Table、DP-Means；对齐「vanilla 坍缩 / 调参无法语义化 / 完整三层高 grounding」趋势。

**成功标准：**

1. Vanilla Gumbel-softmax 离散瓶颈出现符号坍缩（使用符号数远小于 codebook）。
2. 常见 anti-collapse 调参可提高多样性但 grounding accuracy 仍低。
3. 完整三层修复后 grounding 显著高于去掉任一层（方向对齐论文 97.2% vs 去第三层 22.2% 量级对比；绝对数值允许偏差）。

**明确不做：** 真实机器人、大规模 V-JEPA/CLIP 全家桶（可选增强）。

## 2. 环境与依赖

| 项 | 建议 |
|----|------|
| 框架 | PyTorch |
| 最小环境 | 5×5 grid、少量彩色对象、离散动作 |
| 编码器 | 先 CNN；通过后再试冻结 CLIP/V-JEPA |
| 参数量 | 论文称可训练 <2M；本子集保持同量级 |

## 3. 子集实验矩阵（B）

| ID | 设置 | 记录指标 |
|----|------|----------|
| B1 | Vanilla Gumbel-softmax | 活跃符号数、grounding acc |
| B2 | 温度/熵奖励/谱归一等 anti-collapse | 同上 |
| B3 | + `z.detach()` | 是否停止坍缩但仍缺语义 |
| B4 | + Memory Table | 绑定准确率 |
| B5 | + DP-Means（增加对象数制造冲突） | 冲突下准确率；去掉该层的对照 |
| B6 | 多 seed（建议 ≥5） | 均值±方差 |

## 4. 执行步骤

1. 精读 §3 失败实验与 §4 三层定义，画出模块图。
2. 实现 grid world + 离散瓶颈 + 语言/标签监督。
3. 跑 B1–B2，确认负结果可复现。
4. 按顺序叠加三层修复，做去掉一层的 ablation。
5. 将超参（codebook 大小、温度、DP-Means 阈值）与结果写入 `notes/write-protect.md`。

## 5. 验收清单

- [ ] 负结果可复现（坍缩或语义失败）
- [ ] 三层齐全与至少一层 ablation
- [ ] 多 seed 或明确单 seed 局限
- [ ] 无官方代码声明

## 6. Out of Scope

- 宣称已修复真实机器人世界模型语言注入问题
- 端到端 LLM 微调对照（论文强调无需）

## 7. 设计原则（写入结论）

语言可命名与绑定符号，但不得反向写坏物理符号形成层——「写保护边界」是本子集要带走的架构原则。
